import React from 'react'

const ThemeOverview = ({ themeState }) => {
    return (
        <div style={{ fontSize: themeState.buttons.fontSize.styleValueResolved }}>
            hello
        </div>
    )
}

export default ThemeOverview
